<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_root"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="14dp"
    app:cardUseCompatPadding="true"
    app:cardElevation="6dp"
    app:cardBackgroundColor="@color/card_bg"
    app:strokeColor="@color/accent_neon"
    app:strokeWidth="2dp"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/Recycler_View_Item_meal"
        >

        <!-- Top image fills top area -->
        <ImageView
            android:id="@+id/Meal_Image"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:src="@drawable/beef_image"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/info_strip"
         />

        <!-- Bottom info strip -->
        <View
            android:id="@+id/info_strip"
            android:layout_width="0dp"
            android:layout_height="72dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:background="@color/info_strip" />

        <!-- Title on the left inside strip -->
        <TextView
            android:id="@+id/Meal_Name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Classic Burger"
            android:textColor="@color/title_white"
            android:textStyle="bold"
            android:textSize="20sp"
            android:ellipsize="end"
            android:maxLines="2"
            android:paddingStart="14dp"
            app:layout_constraintStart_toStartOf="@id/info_strip"
            app:layout_constraintTop_toTopOf="@id/info_strip"
            app:layout_constraintBottom_toBottomOf="@id/info_strip"
            app:layout_constraintEnd_toStartOf="@+id/meal_details_button"
            android:layout_marginEnd="8dp"
            android:gravity="center_vertical" />

        <!-- circular action button (arrow) bottom-right, with outline -->
        <ImageButton
            android:id="@+id/meal_details_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:scaleType="centerInside"
            android:background="@drawable/bg_circle_outline"
            android:src="@drawable/ic_chevron_right"
            app:layout_constraintEnd_toEndOf="@id/info_strip"
            app:layout_constraintBottom_toBottomOf="@id/info_strip"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="12dp"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
